# Some dummy functions

def get_user_id_by_token(token: str) -> int:
    return 0


def get_token_by_user_id(user_id: int) -> str:
    return "CurUserToken"


def authenticate_user(login: str, password: str) -> int:
    return 0


def update_token(token: str) -> str:
    return token